(() => { const form=document.getElementById("feedback-form"); if(!form)return; const status=document.getElementById("feedback-status"),button=document.getElementById("feedback-button"); form.addEventListener("submit",async(event)=>{event.preventDefault();button.disabled=true;status.textContent="Checking your Discord login…";try{const client=window.RESPONDER_ROYALE_SUPABASE_CLIENT;if(!client)throw new Error("The login service is still loading.");const{data:{session}}=await client.auth.getSession();if(!session)throw new Error("Please sign in with Discord before sending feedback.");const{error}=await client.from("community_feedback").insert({user_id:session.user.id,category:document.getElementById("feedback-type").value,title:document.getElementById("feedback-title").value.trim(),details:document.getElementById("feedback-details").value.trim()});if(error)throw error;form.reset();status.textContent="Thank you — your feedback was received.";}catch(error){status.textContent=error.message||"Feedback could not be sent.";}finally{button.disabled=false;}}); })();
